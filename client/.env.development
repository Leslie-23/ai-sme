# Development — leave VITE_API_URL empty so requests go to /api/* via the
# Vite dev-server proxy (configured in vite.config.ts to forward to the
# local Express server on http://localhost:4000). Setting it here would
# bypass the proxy and trigger CORS preflights locally.
VITE_API_URL=
